<h1> Best Restaurants, ever...super serious </h1>
<div class="card">
  <div class="card-body">
<% @restaurants.each do |restaurant| %>
<h2 class="card-title"><%= link_to restaurant.name, restaurant_path(restaurant[:id]) %></h2>
<%= link_to "add new review", new_restaurant_review_path(restaurant.id), class: "btn btn-secondary" %>
<% end %>
 </div>
</div>
<br>
<br>
<%= link_to "Add a new restaurant", new_restaurant_path, class: "btn btn-primary"%>
